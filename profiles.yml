my_dbt_project:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      port: 5432
      user: dbt_user
      password: dbt_password
      dbname: my_dbt_db
      schema: dbt_dev          # dbt will create this schema and write models here
      threads: 4
